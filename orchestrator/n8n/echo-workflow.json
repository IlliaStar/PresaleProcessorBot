{
  "name": "Presale Agent — Echo Test",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "presale-agent",
        "responseMode": "responseNode",
        "options": {}
      },
      "id": "a1b2c3d4-0001-4000-8000-000000000001",
      "name": "Teams Bot Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [240, 300],
      "webhookId": "presale-agent"
    },
    {
      "parameters": {
        "jsCode": "const body = $input.first().json.body || $input.first().json;\nconst msg = (body.message || '').trim() || '(empty message)';\nconst files = (body.attachments || []).filter(a => a.content).map(a => a.name);\nconst fileNote = files.length ? `\\n\\nFiles received: ${files.join(', ')}` : '';\nreturn [{ json: { reply: `Echo from n8n: \"${msg}\"${fileNote}` } }];"
      },
      "id": "a1b2c3d4-0002-4000-8000-000000000002",
      "name": "Build Echo Reply",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [460, 300]
    },
    {
      "parameters": {
        "respondWith": "firstIncomingItem",
        "options": {}
      },
      "id": "a1b2c3d4-0003-4000-8000-000000000003",
      "name": "Respond to Webhook",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1,
      "position": [680, 300]
    }
  ],
  "connections": {
    "Teams Bot Webhook": {
      "main": [
        [
          {
            "node": "Build Echo Reply",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Build Echo Reply": {
      "main": [
        [
          {
            "node": "Respond to Webhook",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "pinData": {},
  "settings": {
    "executionOrder": "v1"
  },
  "staticData": null,
  "tags": []
}
